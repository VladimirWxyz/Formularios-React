var nombre = "Test";

alert (nombre);

var name;

name = prompt ("Enter your name", "Number")

alert (name);

console.log(name);

document.write(name);

Test = prompt ("Enter name");
Age = prompt ("Age");
Ag = 20 - 3;

document.write(Test + " " + Age + " " + Ag);

//Tipos de datos

    // Numerico

    var edad = 19;

    // String

    var nombre = "Vladimir ";
    var apellido = "Williams ";
    var texto = "Text of \"test\"";

    document.write(nombre + apellido + texto);

    // Array

    var friends = ["Albert", " Vladimir ", "Williams"]

    friends[friends.length] = "Abduzcan";

    document.write(friends[1]);

    document.write("Tienes " + friends.length + " amigos");

    // Object

    var object = {
        color: "Black",
        size: 12
    }

    // Boolean

    var bolean = false;

    //Undefined

    var testeo;
    document.write(testeo);

    // Null

    var numtest1 = 10;
    var numtest1 = null;

    // Nan

    var text1 = "hello";
    document.write(text1 * 2)

    // NaN es un valor invalido en js

    